rules:
  # The integration jobs intentionally consume repository-level RPC secrets.
  secrets-outside-env:
    disable: true
  # tempoxyz/gh-actions uses dated tag comments for its SHA pins.
  ref-version-mismatch:
    disable: true
